# Dev journal

Format: UTC offset from `git`; newest first within a day.

## 2026-05-10

**11:00 -0500** — Phase 10 (`7268337`): Prefetch lazy window chunks on launcher icon + dock button `pointerenter` / `focusin` so first open hits warm cache.

**10:59 -0500** — Phase 9 (`d0c7052`): `openWindow` → async + dynamic `import()` for Browse, Files, Editor, Paint, Snake, Pong, Rubik; split `browser-url.ts`; `vite.config.ts` chunk warning limit for Rubik chunk.

**10:54 -0500** — Phase 8 (`999b6ef`): Matrix preference in `localStorage`; default off under `prefers-reduced-motion` when unset; skip-to-terminal link + `TerminalApp.focusShell()`; catalog copy note.

**10:51 -0500** — Phase 7 (`bc211e7`): Mobile ≤720 browser URL row + file explorer toolbar stack; idle-deferred matrix init (+ early `syncSettingsSoundToggle`); `#desktop-workspace` `role="main"`, launcher `role="region"`; bookmarks bar horizontal scroll narrow.

## 2026-05-10 (earlier)

**10:48 -0500** — Phase 6 (`17c1b7e`): In-app polish pass (POLISH checklist): chrome tokens / focus-visible, résumé column, empty states, bookmarks bar, snake/rubik focus.

**10:35–10:41 -0500** — UI passes: motion polish (`103bbc9`), system menu theme dropdown (`2c7c9df`), menu shortcuts tidy (`b33e3ad`).

**10:29 -0500** — Phase 4 surface (`bf5098c`): Terminal/help polish pass.
